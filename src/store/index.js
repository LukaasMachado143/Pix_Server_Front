import Vue from 'vue'
import Vuex from 'vuex'
import snackbarStore from './snackbar-store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    snackbarStore
  }
})
